{
  "name": "sensu-keepalive-gatd",
  "version": "1.0.0",
  "description": "Log sensu keepalive status to GATD",
  "main": "sensu-keepalive-gatd.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Branden Ghena",
  "license": "ISC",
  "dependencies": {
    "amqp": "^0.2.6",
    "fs": "0.0.2",
    "ini": "^1.3.4",
    "request": "^2.72.0",
    "watchout": "0.0.5"
  }
}
